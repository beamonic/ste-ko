# STE-KO 작성 규칙

버전 0.2.0 (2026-08-15)

이 문서는 규칙 본문이다. 어휘 대응표는 [dictionary.md](dictionary.md)에 있다.

규칙마다 번호가 붙는다. 리뷰에서 "4.3 위반"처럼 번호로 지적한다. 번호는 버전이 올라가도 바뀌지 않는다. 규칙을 없앨 때는 번호를 지우지 않고 `폐기`로 표시한다.

---

## 0 총칙

### 0.1 적용 범위

네 가지 표면에 적용한다. `문서`, `대화`, `코딩`, `UI`다. 표면마다 달라지는 부분은 10장에 있다.

- `문서`: 기술 문서, 상태 보고, 원인 진단, 절차서, 안전 지침
- `대화`: 사람과 주고받는 AI 답변, 채팅, 리뷰 코멘트
- `코딩`: 커밋 메시지, PR 본문, 코드 주석, 에이전트 작업 보고
- `UI`: 버튼, 라벨, 안내 문구, 빈 화면, 오류 메시지

적용하지 않는 곳: 코드 자체, 로그, 명령어, 법률 문구, 계약서, 인용문, 홍보 문안, 사적인 대화.

표면을 밝히지 않으면 `문서`로 본다.

### 0.2 목표

읽는 사람이 한 번 읽고 다음 행동을 정하게 한다. 문장이 두 가지로 읽히면 안 된다.

짧게 쓰는 것은 목표가 아니라 수단이다. 판단에 필요한 근거를 지우면서까지 줄이지 않는다.

### 0.3 규칙 충돌 우선순위

두 규칙이 부딪히면 위쪽이 이긴다.

1. 안전 (7장)
2. 정확성 — 사실, 수치, 불확실성, 실패 (6장)
3. 모호성 제거 (1-4장)
4. 분량 압축 (9장)

압축은 항상 마지막이다. 9장 때문에 7장을 어기면 규칙 위반이다.

### 0.4 준수 등급

| 등급 | 뜻 |
|---|---|
| `필수` | 어기면 규칙 위반이다. |
| `기본` | 지키되, 어길 때는 이유를 밝힌다. |
| `권고` | 판단에 맡긴다. |

표시가 없는 규칙은 `기본`이다.

---

## 1 단어

### 1.1 한 단어에 뜻 하나 `필수`

한 문서 안에서 한 단어는 한 가지 뜻으로만 쓴다.

> 나쁨: "배포가 실패해서 배포를 다시 배포했다." (`배포`가 행위, 결과, 재실행 세 뜻)
> 좋음: "배포가 실패했다. 같은 커밋을 다시 올렸다."

### 1.2 한 대상에 단어 하나 `필수`

같은 대상은 항상 같은 단어로 부른다. 글이 지루해 보인다는 이유로 동의어를 바꿔 쓰지 않는다.

> 나쁨: "설정 파일을 고쳤다. 환경설정을 반영하려면 세팅을 다시 읽어야 한다."
> 좋음: "설정 파일을 고쳤다. 설정 파일을 반영하려면 서버를 다시 시작한다."

이 규칙 하나가 읽기 어려움의 가장 큰 원인을 없앤다. 읽는 사람은 단어가 바뀌면 대상도 바뀌었다고 읽는다.

### 1.3 비승인 어휘 금지 `필수`

[dictionary.md](dictionary.md)에서 `비승인`으로 표시한 표현을 쓰지 않는다. 대응하는 승인 표현으로 바꾼다.

### 1.4 고유 식별자는 그대로 둔다 `필수`

함수명, 변수명, 명령어, 파일 경로, 오류 클래스, 제품명, 법 조문 번호, HTTP 상태 코드는 번역하거나 풀어 쓰지 않는다. 코드 서식으로 감싼다.

> 나쁨: "권한 없음 오류가 났다."
> 좋음: "`403 Forbidden`이 났다."

### 1.5 약어는 처음 한 번만 푼다

약어가 처음 나올 때 한 번 풀어 쓴다. 그다음부터는 약어만 쓴다. 문서 안에서 푼 형태와 약어를 번갈아 쓰지 않는다.

> 좋음: "CDN(콘텐츠 전송망)을 껐다. CDN을 다시 켜면 캐시가 비워진다."

### 1.6 쉬운 말이 있으면 쉬운 말을 쓴다

뜻이 같고 정확도가 떨어지지 않으면 익숙한 말을 쓴다. 정확도가 떨어지면 어려운 말을 그대로 둔다.

> 나쁨: "배포에 30분이 소요된다."
> 좋음: "배포에 30분이 걸린다."
> 그대로 둠: "이 작업은 멱등하다." — 뜻을 처음 나올 때 설명한다.

### 1.7 전문 용어는 결과로 설명한다

용어를 처음 쓸 때 그 용어가 읽는 사람에게 무엇을 뜻하는지 한 문장으로 붙인다. 사전 정의를 옮기지 않는다.

> 나쁨: "멱등성이란 연산을 여러 번 적용해도 결과가 달라지지 않는 성질이다."
> 좋음: "이 API는 멱등하다. 같은 요청을 두 번 보내도 데이터는 한 번만 바뀐다."

### 1.8 비유와 관용구를 쓰지 않는다 `필수`

기술 문서에 비유, 관용구, 의성어, 의태어를 쓰지 않는다. 번역이 되지 않고 뜻이 사람마다 다르다.

> 나쁨: "서버가 뻗었다."
> 좋음: "서버가 응답을 멈췄다."

### 1.9 접미사를 남용하지 않는다

`-적`, `-화`, `-성`, `-들`, `-시`, `-내`, `-간`은 뜻이 달라질 때만 쓴다. 빼도 뜻이 같으면 뺀다.

> 나쁨: "기술적 이슈들로 인해 안정화 작업이 지연되고 있는 상황입니다."
> 좋음: "오류 두 건 때문에 작업이 하루 늦어집니다."

---

## 2 명사구

한국어에서 가장 자주 깨지는 곳이다. 명사를 계속 이어 붙일 수 있어서, 문법에 어긋나지 않으면서 뜻이 사라진다.

### 2.1 명사 나열은 3개까지 `필수`

조사 없이 잇는 명사는 3개를 넘지 않는다.

> 나쁨: "사용자 인증 토큰 갱신 실패 처리 로직" (명사 7개)
> 좋음: "인증 토큰 갱신이 실패했을 때 실행하는 코드"

### 2.2 4개 이상이면 조사나 절로 푼다 `필수`

2.1을 어길 수밖에 없으면 `-의`, `-을 위한`, `-하는` 같은 연결로 끊는다. 끊는 자리는 뜻의 경계에 둔다.

> 나쁨: "결제 실패 알림 발송 배치 작업"
> 좋음: "결제가 실패하면 알림을 보내는 배치 작업"

### 2.3 관형절은 2개까지

`-하는`, `-한`, `-인`으로 끝나는 꾸밈절을 한 명사 앞에 3개 이상 겹치지 않는다.

> 나쁨: "어제 배포한 수정된 설정이 적용된 서버"
> 좋음: "어제 배포한 서버. 이 서버에는 고친 설정이 적용됐다."

### 2.4 형식 명사를 남용하지 않는다

`-하는 것`, `-라는 것`, `-인 것`, `-할 수 있음`은 뜻이 달라질 때만 쓴다.

> 나쁨: "재시도하는 것이 필요한 것으로 판단됩니다."
> 좋음: "재시도해야 합니다."

### 2.5 동사를 명사로 바꾸지 않는다 `필수`

동작은 동사로 쓴다. 명사로 바꾸고 빈 동사를 붙이지 않는다.

> 나쁨: "삭제를 진행하겠습니다." / "검증을 수행했습니다."
> 좋음: "삭제하겠습니다." / "검증했습니다."

---

## 3 동사

### 3.1 능동태를 쓰고 행위자를 밝힌다 `필수`

누가 했는지 쓴다. 행위자를 모르면 모른다고 쓴다.

> 나쁨: "설정이 변경되었습니다."
> 좋음: "배포 스크립트가 설정을 바꿨습니다."
> 모를 때: "설정이 바뀌었습니다. 누가 바꿨는지는 아직 확인하지 못했습니다."

피동을 쓸 수 있는 경우는 셋뿐이다. 행위자를 알 수 없을 때, 행위자가 중요하지 않을 때, 대상이 문장의 주제일 때.

### 3.2 이중피동 금지 `필수`

`-되어지다`, `-보여지다`, `-불려지다` 같은 이중피동을 쓰지 않는다. 전체 목록은 dictionary.md B절에 있다.

> 나쁨: "결과가 화면에 보여집니다."
> 좋음: "화면에 결과가 나옵니다."

### 3.3 상태를 숨기는 피동을 쓰지 않는다 `필수`

`처리되었습니다`, `조치되었습니다`, `반영되었습니다`는 무엇이 어떻게 됐는지 말하지 않는다. 주어와 동작을 밝힌다.

> 나쁨: "요청하신 건은 처리되었습니다."
> 좋음: "환불 3건을 승인했습니다. 카드사 반영에 3영업일이 걸립니다."

### 3.4 사동을 겹치지 않는다

`-하게 하다`, `-시키다`를 필요 없이 붙이지 않는다.

> 나쁨: "서버를 재시작시켰습니다."
> 좋음: "서버를 재시작했습니다."

### 3.5 시제는 셋만 쓴다

과거, 현재, 미래만 쓴다. `-하고 있었었다`, `-하게 될 것이었다` 같은 겹친 시제를 쓰지 않는다.

### 3.6 `-하도록 하다`를 쓰지 않는다 `필수`

> 나쁨: "로그를 남기도록 하겠습니다."
> 좋음: "로그를 남기겠습니다."

### 3.7 추측과 확인을 동사로 구분한다 `필수`

확인한 사실, 추론, 확인하지 못한 것을 동사로 갈라 쓴다. 6.2와 함께 쓴다.

| 상태 | 쓰는 형태 |
|---|---|
| 확인함 | `-다`, `-했다`, `-이다` |
| 추론함 | `-로 보인다`, `-일 가능성이 높다` + 근거 |
| 확인 못 함 | `아직 확인하지 못했다` |

`-인 것 같습니다`는 세 상태 중 어느 것인지 알 수 없다. 쓰지 않는다.

---

## 4 문장

### 4.1 길이 상한 `필수`

| 문장 종류 | 상한 |
|---|---|
| 절차문 (5장) | 20어절 |
| 설명문 (6장) | 25어절 |
| 안전문 (7장) | 20어절 |

어절은 띄어쓰기 단위로 센다. 상한을 넘으면 문장을 자른다. 조사를 빼서 줄이지 않는다.

### 4.2 한 문장에 뜻 하나 `필수`

한 문장은 한 가지를 말한다. `그리고`로 이을 수 있으면 두 문장이다.

### 4.3 연결어미로 3절 이상 잇지 않는다 `필수`

`-고`, `-며`, `-는데`, `-지만`, `-어서`로 이은 절이 한 문장에 3개를 넘지 않는다.

> 나쁨: "배포가 실패했는데 로그를 보니 마이그레이션이 안 돌았고 그래서 테이블이 없었으며 롤백은 됐지만 캐시는 남아 있습니다."
> 좋음: "배포가 실패했습니다. 마이그레이션이 실행되지 않아 테이블이 없었습니다. 롤백은 끝났습니다. 캐시는 아직 남아 있습니다."

### 4.4 주어와 서술어를 가깝게 둔다

주어와 서술어 사이에 꾸밈말을 길게 넣지 않는다. 길어지면 꾸밈말을 앞 문장으로 뺀다.

### 4.5 이중부정을 쓰지 않는다 `필수`

> 나쁨: "확인되지 않은 것은 아닙니다."
> 좋음: "확인했습니다."

### 4.6 조사를 생략하지 않는다

특히 `-은/는`과 `-이/가`를 구분해서 쓴다. 대비면 `-은/는`, 새 정보면 `-이/가`를 쓴다.

### 4.7 문단은 6문장까지

한 문단에 한 주제만 담는다. 주제가 바뀌면 문단을 나눈다.

### 4.8 문장 길이와 종결어미를 섞는다

4.1은 상한이지 목표가 아니다. **하한은 없다.** 모든 문장을 최소 길이로 자르지 않는다.

같은 종결어미를 세 문장 연속 쓰지 않는다. 연속되면 두 문장을 하나로 합치거나 어순을 바꾼다.

세 항목 나열을 습관으로 쓰지 않는다. 항목이 둘이면 둘로, 넷이면 넷으로 쓴다.

> 나쁨: "배포가 실패했습니다. 원인은 인증서입니다. 갱신이 꺼져 있었습니다. 오늘 고치겠습니다."
> 좋음: "배포가 실패했습니다. 인증서 자동 갱신이 6월부터 꺼져 있었던 것이 원인입니다. 오늘 고치겠습니다."

이 규칙은 4.1, 4.2와 부딪히는 것처럼 보이지만 부딪히지 않는다. 4.1은 상한이고 4.8은 분포다. 상한 안에서 길이를 섞는다.

**이 규칙이 없으면 규격이 자기 부작용을 만든다.** 4.1과 4.2를 기계적으로 지키면 같은 어미로 끝나는 단문이 줄줄이 이어진다. 그 문체는 읽기 쉬워지지 않고 기계가 쓴 것처럼 읽힌다.

---

## 5 절차문

읽는 사람이 그대로 따라 하는 글이다.

### 5.1 한 단계에 한 동작 `필수`

한 번호에 동작 하나만 넣는다. 동작이 둘이면 번호를 둘로 나눈다.

### 5.2 명령형으로 쓴다 `필수`

`-하십시오` 또는 `-한다` 중 하나를 골라 문서 전체에서 통일한다. `-하시면 됩니다`, `-하는 것이 좋습니다`를 쓰지 않는다.

> 나쁨: "서버를 재시작하시면 됩니다."
> 좋음: "서버를 재시작하십시오."

### 5.3 조건을 동작 앞에 둔다 `필수`

조건이 있으면 조건을 먼저 쓴다. 읽는 사람이 동작을 실행한 뒤에 조건을 읽으면 늦다.

> 나쁨: "백업이 없으면 이 명령을 실행하지 마십시오."
> 좋음: "백업이 있는지 먼저 확인하십시오. 백업이 없으면 멈추십시오. 백업이 있으면 이 명령을 실행하십시오."

### 5.4 순서가 있으면 번호를 쓴다

순서가 있으면 번호 목록, 없으면 글머리표를 쓴다. 순서가 없는데 번호를 쓰지 않는다.

### 5.5 확인 방법을 적는다 `필수`

각 단계가 성공했는지 읽는 사람이 어떻게 아는지 쓴다.

> 좋음: "3. `systemctl status api`를 실행하십시오. `active (running)`이 나오면 성공입니다."

---

## 6 설명문

상태 보고, 원인 진단, 리뷰에 쓴다.

### 6.1 결론을 먼저 쓴다 `필수`

첫 문장이나 두 번째 문장 안에 결론과 현재 상태가 나와야 한다. 조사 과정, 시도한 방법, 배경은 그다음이다.

> 나쁨: "먼저 로그를 확인했고, 그다음 설정을 비교했으며, 여러 가능성을 검토한 결과…"
> 좋음: "결제 API가 어제 21시부터 죽어 있습니다. 원인은 만료된 인증서입니다."

### 6.2 사실, 추론, 미확인을 구분한다 `필수`

세 가지를 섞지 않는다. 3.7의 동사 형태를 쓴다. 근거가 있으면 근거를 붙인다.

> 좋음: "로그에 `SSLCertVerificationError`가 217번 찍혔습니다(확인). 인증서 자동 갱신이 6월에 꺼진 것으로 보입니다(추론, 갱신 로그가 6월 이후 없음). 누가 껐는지는 확인하지 못했습니다."

### 6.3 수치에 단위와 기준 시점을 붙인다 `필수`

숫자를 쓸 때 단위, 측정 시각, 측정 범위를 함께 쓴다. 비교값이면 비교 대상도 쓴다.

> 나쁨: "응답이 많이 느려졌습니다."
> 좋음: "p95 응답 시간이 8월 14일 21시부터 220ms에서 1,900ms로 늘었습니다."

### 6.4 실패와 미완료를 지우지 않는다 `필수`

짧게 쓴다는 이유로 실패, 부분 성공, 건너뛴 단계를 빼지 않는다. 끝나지 않은 일을 끝난 것처럼 쓰지 않는다.

---

## 7 안전문

되돌릴 수 없는 동작, 데이터 삭제, 돈, 권한, 개인정보, 의료, 법률에 적용한다.

7장은 다른 모든 장보다 우선한다(0.3). 9장의 압축을 적용하지 않는다.

### 7.1 경고를 동작 앞에 둔다 `필수`

경고는 그 동작을 설명하기 전에 나와야 한다. 뒤에 붙이지 않는다.

### 7.2 위험, 결과, 회피 순서로 쓴다 `필수`

무엇이 위험한지, 어기면 무엇이 일어나는지, 어떻게 피하는지를 이 순서로 쓴다.

> 좋음: "이 명령은 `users` 테이블의 모든 행을 지웁니다. 되돌릴 수 없습니다. 실행 전에 `pg_dump`로 백업하고 백업 파일 크기를 확인하십시오."

### 7.3 되돌릴 수 없음을 명시한다 `필수`

되돌릴 수 없으면 "되돌릴 수 없습니다"라고 그대로 쓴다. `주의하십시오`, `신중히 검토하십시오`로 대신하지 않는다.

### 7.4 경고를 완곡하게 쓰지 않는다 `필수`

`-할 수도 있습니다`, `가급적`, `되도록`을 안전문에 쓰지 않는다.

> 나쁨: "데이터가 손실될 수도 있으니 가급적 백업을 권장합니다."
> 좋음: "백업이 없으면 데이터를 복구할 수 없습니다. 백업하십시오."

---

## 8 문장부호

### 8.1 물결표를 범위에 쓰지 않는다 `필수`

`3~5개`처럼 쓰지 않는다. GitHub, Slack, Discord를 비롯한 대부분의 마크다운은 홑물결표를 취소선으로 읽는다. 문서가 깨지고, 정적 사이트 빌드가 실패한다.

> 나쁨: `3~5개`
> 좋음: `3-5개`, `3개에서 5개`, `3개 이상 5개 이하`

### 8.2 괄호를 겹치지 않는다 `필수`

괄호 안에 괄호를 넣지 않는다. 괄호가 두 겹이면 문장을 나눈다.

### 8.3 쉼표로 절을 3개 넘게 잇지 않는다

4.3과 같은 상한을 쉼표에도 적용한다.

### 8.4 슬래시를 `및/또는` 뜻으로 쓰지 않는다 `필수`

`/`는 두 가지로 읽힌다. `그리고`인지 `또는`인지 단어로 쓴다.

> 나쁨: "관리자/운영자가 승인합니다."
> 좋음: "관리자 또는 운영자가 승인합니다."

경로(`/etc/hosts`)와 단위(`10건/초`)는 예외다.

### 8.5 말줄임표로 문장을 끝내지 않는다 `필수`

`...`은 무엇을 생략했는지 말하지 않는다. 생략한 내용을 쓰거나 문장을 끝낸다.

---

## 9 압축 등급

케이브맨(caveman)에서 가져온 부분이다. 1-8장이 모호성을 없애고, 9장이 분량을 줄인다. 순서가 중요하다. 모호한 문장을 먼저 압축하면 더 모호해진다.

압축은 규칙 우선순위에서 가장 낮다(0.3).

### 9.1 등급

| 등급 | 무엇이 달라지나 |
|---|---|
| `표준` | 1-8장만 적용한다. 완전한 문장을 쓴다. 기본값이다. |
| `압축` | 인사말, 완충어, 자기 서술을 지운다. 조사 일부를 생략한다. 문장 조각을 허용한다. |
| `최소` | 위에 더해 연결어를 화살표(`→`)로 바꾸고, 아는 약어를 그대로 쓰고, 한 줄에 한 사실만 남긴다. |

### 9.2 압축 등급에서 지우는 것 `필수`

인사말, 자기 서술, 완충어, 되풀이를 지운다. 목록은 dictionary.md G절에 있다.

작업 과정 서술도 지운다. "확인해 보겠습니다", "파일을 열어 보겠습니다"를 쓰지 않고 결과를 바로 쓴다.

장식용 표와 이모지를 지운다. 표는 항목이 3개를 넘고 열이 2개를 넘을 때만 쓴다.

로그와 오류 출력을 통째로 붙이지 않는다. 판단을 가르는 한 줄만 인용한다. 전체가 필요하면 읽는 사람이 요청한다.

약어를 새로 만들지 않는다. 이미 널리 쓰는 약어만 쓴다(1.5).

> 표준: "말씀하신 부분을 확인해 보았습니다. 확인 결과, 인증 미들웨어에서 토큰 만료 검사가 `<`로 되어 있어 문제가 발생하는 것으로 보입니다."
> 압축: "인증 미들웨어 버그입니다. 토큰 만료 검사가 `<=`가 아니라 `<`입니다."
> 최소: "인증 미들웨어. 만료 검사 `<` → `<=` 여야 함."

### 9.3 압축해도 지우지 않는 것 `필수`

압축 등급에서도 다음은 그대로 둔다.

- **부정과 한정을 나타내는 말.** `안`, `못`, `-지 않다`, `-지 못하다`, `없다`, `아니다`, `만`, `-뿐`, `제외`, `-을 빼고`는 절대 지우지 않는다. 뜻이 뒤집히는 손해가 아낀 분량보다 항상 크다. 한국어에서는 부정 표지가 짧아서 압축할 때 가장 먼저 사라진다. 이 항목이 9장에서 가장 위험한 자리다.
- 안전 경고 전체 (7장)
- 수치, 단위, 기준 시점 (6.3)
- 사실과 추론의 구분 (6.2)
- 실패와 미완료 (6.4)
- 고유 식별자, 코드, 오류 메시지 원문 (1.4)

### 9.4 압축을 자동으로 푸는 조건 `필수`

다음에 해당하면 등급과 상관없이 `표준`으로 돌아간다.

- 되돌릴 수 없는 동작을 설명할 때
- 안전, 법률, 의료, 금전, 권한을 다룰 때
- 절차가 3단계를 넘고 순서를 잘못 읽으면 위험할 때
- 압축 자체가 새 모호성을 만들 때. 줄인 문장이 두 가지로 읽히면 줄이기 전으로 돌린다.
- 읽는 사람이 같은 질문을 다시 하거나 못 알아들었다고 말할 때

해당 부분이 끝나면 원래 등급으로 돌아간다.

### 9.5 압축은 명사 나열을 늘리지 않는다 `필수`

압축한다고 2.1의 명사 나열 상한을 어기지 않는다. 어절을 줄이려고 조사를 빼면 뜻이 사라진다. 이때는 문장을 나눈다.

> 나쁨(압축이 2.1을 깸): "인증 토큰 갱신 실패 처리 코드 수정 필요"
> 좋음: "인증 토큰 갱신이 실패한다. 처리 코드를 고쳐야 한다."

---

## 10 적용 표면

9장이 분량을 정한다면 10장은 자리를 정한다. 같은 규칙도 기술 문서와 버튼 라벨에서 다르게 걸린다.

표면을 먼저 고르고, 그다음 압축 등급을 고른다. 1-8장은 모든 표면에 그대로 적용한다. 10장은 표면마다 달라지는 부분만 정의한다.

### 10.1 표면 목록 `필수`

| 표면 | 쓰는 자리 | 기본 압축 등급 |
|---|---|---|
| `문서` | 기술 문서, 절차서, 장애 보고, API 문서 | `표준` |
| `대화` | 사람과 주고받는 AI 답변, 채팅, 리뷰 코멘트 | `압축` |
| `코딩` | 커밋 메시지, PR 본문, 코드 주석, 에이전트 작업 보고 | `압축` |
| `UI` | 버튼, 라벨, 안내 문구, 빈 화면, 오류 메시지 | `최소` |

표면을 밝히지 않으면 `문서`로 본다.

### 10.2 표면별로 달라지는 것

| 규칙 | `문서` | `대화` | `코딩` | `UI` |
|---|---|---|---|---|
| 4.1 문장 상한 | 25어절 | 25어절 | 20어절 | 12어절 |
| 4.7 문단 상한 | 6문장 | 4문장 | 4문장 | 2문장 |
| 1.5 약어 풀기 | 처음 한 번 | 처음 한 번 | 생략 가능 | 생략 가능 |
| 5.2 종결어미 통일 범위 | 문서 하나 | 대화 하나 | 저장소 하나 | 제품 하나 |
| 9장 기본 등급 | `표준` | `압축` | `압축` | `최소` |

7장 전체와 9.3은 표면과 상관없이 그대로 걸린다. 표면을 바꾼다는 이유로 안전 경고와 부정 표현을 줄이지 않는다.

### 10.3 되묻기는 한 번에 하나 `필수` `대화`

한 답변에 질문을 두 개 넘게 붙이지 않는다. 질문이 늘면 읽는 사람은 하나만 답하거나 아무것도 답하지 않는다.

> 나쁨: "A로 할까요, B로 할까요? 그리고 배포는 오늘 할까요?"
> 좋음: "A로 하겠습니다. 배포 시점만 정해 주십시오."

선택지는 3개까지 준다. 넘으면 하나를 추천하고 이유를 한 줄 쓴다.

### 10.4 앞 턴을 되풀이하며 시작하지 않는다 `필수` `대화`

상대가 방금 한 말을 요약하면서 답을 열지 않는다. 첫 문장이 답이다(6.1).

> 나쁨: "말씀하신 배포 실패 건을 확인해 달라는 요청으로 이해했습니다. 확인해 보겠습니다."
> 좋음: "인증서가 8월 14일에 만료됐습니다."

동의를 표시하며 문장을 시작하지 않는다. `좋은 질문입니다`, `정확한 지적입니다`, `말씀하신 대로`를 쓰지 않는다.

### 10.5 모르면 첫 문장에서 모른다고 쓴다 `필수` `대화`

확인하지 못한 것을 답 뒤쪽에 숨기지 않는다. 6.2의 대화 적용이다.

> 나쁨: "대략 이런 구조로 동작합니다. (설명 12줄) 다만 실제 코드는 확인하지 못했습니다."
> 좋음: "이 저장소를 읽지 못했습니다. 일반적인 구조로만 답합니다."

### 10.6 무엇을 왜 바꿨는지 쓴다 `필수` `코딩`

커밋 메시지와 PR 본문에는 바꾼 것과 이유를 쓴다. 어떻게 바꿨는지는 코드가 이미 말한다.

> 나쁨: "리팩터링 및 코드 정리"
> 좋음: "토큰 만료 검사를 `<`에서 `<=`로 고침. 만료 당일 로그인이 막혔음."

### 10.7 위치는 `파일:줄`로 쓴다 `코딩`

`인증 부분`, `그 함수`라고 쓰지 않는다. `app/auth/token.rb:42`처럼 쓴다. 1.4에 따라 식별자는 그대로 둔다.

### 10.8 실행하지 않은 검사를 통과했다고 쓰지 않는다 `필수` `코딩`

에이전트가 가장 자주 어기는 규칙이다. 6.2와 6.4의 코딩 적용이다.

> 나쁨: "테스트를 추가했고 전부 통과합니다." (실행하지 않았음)
> 좋음: "테스트를 3개 추가했습니다. 아직 실행하지 않았습니다."

빌드, 배포, 마이그레이션도 같다. 돌린 것과 안 돌린 것을 나눠서 쓴다.

### 10.9 남긴 일을 목록으로 쓴다 `필수` `코딩`

작업을 끝냈다고 보고할 때 건너뛴 것, 실패한 것, 다음 사람이 할 것을 함께 쓴다. 6.4의 코딩 적용이다.

### 10.10 버튼은 사용자가 하는 동작을 쓴다 `필수` `UI`

`확인`과 `취소`는 무엇이 일어나는지 말하지 않는다. 동사로 쓴다.

> 나쁨: `확인` / `취소`
> 좋음: `삭제` / `유지`

### 10.11 UI 어절 상한 `필수` `UI`

| 자리 | 상한 |
|---|---|
| 버튼, 탭, 메뉴 | 3어절 |
| 라벨, 제목 | 5어절 |
| 안내 문장, 오류 메시지 한 문장 | 12어절 |

상한을 넘으면 문장을 자르지 말고 화면을 나눈다.

### 10.12 오류 메시지는 원인과 다음 동작을 쓴다 `필수` `UI`

무엇이 실패했는지, 사용자가 무엇을 하면 되는지 쓴다. 오류 코드만 쓰지 않는다. 7.2의 UI 적용이다.

> 나쁨: "오류가 발생했습니다. (E-1042)"
> 좋음: "파일이 20MB를 넘습니다. 20MB 아래로 줄여서 다시 올리십시오."

원인을 밝힐 수 없으면 그 사실과 다음 동작을 쓴다. "잠시 뒤 다시 시도하십시오"만 쓰지 않는다.

### 10.13 사용자를 탓하지 않는다 `필수` `UI`

사용자가 무엇을 잘못했는지 쓰지 않고, 무엇을 하면 되는지 쓴다.

> 나쁨: "잘못된 값을 입력했습니다."
> 좋음: "날짜는 `2026-08-15` 형식으로 입력하십시오."

### 10.14 UI에서도 부정과 되돌릴 수 없음을 지우지 않는다 `필수` `UI`

9.3과 7.3의 재확인이다. `UI` 표면은 기본 등급이 `최소`라 가장 위험하다. 자리가 좁다는 이유로 부정 표지와 되돌릴 수 없다는 사실을 지우지 않는다.

> 나쁨: `초기화`
> 좋음: `초기화 (되돌릴 수 없음)`

---

## 부록 A. 리뷰 점검표

문서를 넘기기 전에 확인한다.

- [ ] 표면을 정했다. 밝히지 않았으면 `문서`다 (10.1)
- [ ] 결론이 처음 두 문장 안에 있다 (6.1)
- [ ] 같은 대상을 같은 단어로 불렀다 (1.2)
- [ ] 명사를 4개 이상 이어 붙인 곳이 없다 (2.1)
- [ ] 이중피동이 없다 (3.2)
- [ ] `-되었습니다`로 끝나면서 주어가 없는 문장이 없다 (3.3)
- [ ] 25어절을 넘는 문장이 없다 (4.1)
- [ ] 같은 종결어미가 세 문장 연속으로 나오지 않는다 (4.8)
- [ ] 사실, 추론, 미확인이 구분된다 (6.2)
- [ ] 모든 수치에 단위와 기준 시점이 있다 (6.3)
- [ ] 실패와 미완료를 적었다 (6.4)
- [ ] 되돌릴 수 없는 동작에 그렇게 적었다 (7.3)
- [ ] 줄이면서 부정 표현을 지우지 않았다 (9.3)
- [ ] 표면별 상한을 넘지 않았다 (10.2, 10.11)
- [ ] 범위에 물결표를 쓰지 않았다 (8.1)
- [ ] 비승인 어휘가 없다 (1.3, dictionary.md)

## 부록 B. ASD-STE100과 다른 점

STE-KO는 ASD-STE100의 번역이 아니다. 구조와 목적만 참고한 별개 규격이다. 자세한 내용은 [README.md](README.md)의 저작권 항목을 읽는다.

주요 차이는 셋이다.

**승인 어휘 목록이 없다.** ASD-STE100은 약 900개의 승인 단어만 쓰게 한다. 한국어는 조사와 어미가 붙어 형태가 바뀌므로 같은 방식이 작동하지 않는다. STE-KO는 대신 비승인 표현과 대체어를 짝지은 대응표를 쓴다.

**한국어에만 있는 문제를 다룬다.** 명사 나열(2장), 이중피동(3.2), 번역투 조사(dictionary.md A절)는 영어 규격에 없다.

**압축 등급이 있다.** ASD-STE100에는 없다. 케이브맨에서 가져왔다.

## 부록 C. 왜 한국어를 직접 통제하는가

**선택지는 둘이었다.**

A안은 한국어 문장에 규칙을 직접 건다. 지금 규격이 이것이다.

B안은 ASD-STE100으로 영어를 통제해서 쓰고, 한국어는 번역 산출물로 둔다. 승인 어휘 900개 목록이 그대로 작동하고 검증 도구도 이미 있으므로 겉보기에는 더 안전하다.

**B안을 택하지 않았다. 자기모순이기 때문이다.**

번역투는 영어 구조를 한국어에 옮길 때 생긴다. B안은 정확히 그 파이프라인이므로 번역투를 구조적으로 보장한다. dictionary.md A절 전체가 그 파이프라인이 만들어 낸 결과물을 걷어내는 목록이다. 없애려는 것을 만드는 공정을 규격으로 삼을 수는 없다.

번역 단계가 새 모호성을 만든다는 문제도 있다. 통제 영어가 없앤 모호성이 번역에서 되살아나면, 규격을 지킨 문서가 지키지 않은 문서보다 나을 근거가 사라진다. 그리고 이 규격의 주 사용처는 AI 에이전트 답변이다. 영어로 쓰고 번역하는 두 단계는 지연과 비용을 늘린다.

**"한국어는 변수가 너무 많다"는 지적은 절반만 맞다.**

변이는 **형태**에 몰려 있고, 규칙이 겨냥하는 것은 대부분 **구조**다. 둘을 갈라야 답이 나온다.

| | 변이 | 규칙이 잡는가 |
|---|---|---|
| 형태 | 활용, 조사 교체, 높임, 띄어쓰기 | 어간으로 적으면 잡는다 |
| 구조 | 거의 없다 | 그대로 잡는다 |

명사가 몇 개 연달아 붙었는지(2.1), 서술어가 몇 개인지(4.2), 피동 접사가 몇 겹인지(3.2), 문장이 몇 어절인지(4.1)는 형태가 어떻게 변하든 달라지지 않는다. 규칙 55개 중 다수가 이런 구조 규칙이다.

형태 변이가 실제로 문제가 되는 곳은 어휘 목록 하나뿐이다. 그래서 두 가지로 푼다.

첫째, **승인 어휘 목록을 두지 않는다.** 승인 방식은 허용 형태를 전부 나열해야 하므로 한국어에서 성립하지 않는다. 비승인 방식은 문제 어간만 적으면 되므로 성립한다.

둘째, **어휘 목록을 어간으로 적는다.** `-에 대하-` 한 줄이 `대하여`, `대해`, `대해서`, `대한`, `대하는`을 전부 덮는다. 활용형을 낱개로 적으면 반드시 빠진다. 0.1.0 첫 판이 그 실수를 했고 `통해`, `위해`, `의해`, `관해`가 통째로 누락됐다.

**남는 한계는 적어 둔다.** 형태소 분석 없이 기계 검사를 하면 어간 문자열이 다른 단어에 우연히 걸릴 수 있다. 사람 리뷰에서는 문제가 되지 않고, 나중에 검사 스크립트를 만들 때 형태소 분석기를 붙이면 사라진다.
