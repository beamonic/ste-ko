# 태스크 서술

칸반과 업무 트래커의 항목을 쓰는 규격이다. Notion 칸반, Asana, GitHub Issue에 같게 적용한다. 표면은 `문서`를 쓴다.

## 원리

완료를 판정할 수 있게 쓴다. 읽는 사람이 "무엇이 있으면 닫는가"를 되묻지 않아야 한다.

배경과 행동을 나눈다. 배경이 없는 태스크는 위임이 안 되고, 행동이 없는 태스크는 착수가 안 된다.

## 템플릿

```
제목: [행위자]가 [대상]을 [동사한다]
왜: 이 태스크가 없으면 [무엇]이 막힌다.
DoD: [증거물 링크나 수치]가 있으면 닫는다. 기준 시점을 적는다.
다음 행동: [행위자]가 지금 [첫 행동 하나]를 한다.
```

큰 태스크는 두 칸을 더 채운다. `투입 상한`(이 이상 쓰지 않는다)과 `하지 않는 것`(범위 밖 목록)이다.

## 걸리는 규칙

- 6.1 결론 먼저: 제목이 곧 결과다. 명사 나열 제목은 완료 상태를 가르지 못한다.
- 3.1 행위자 명시: `처리 필요`가 아니라 누가 무엇을 하는지 쓴다.
- 2.1 명사 나열 3개까지: `TM 계약 발행 상태 확인 건` 같은 제목을 막는다.
- 10.8 실행하지 않은 검사 금지: 상태 필드에 거짓 완료를 쓰지 않는다.
- 9장 지우지 않는 것: 수치, 기준 시점, 실패는 줄여도 남긴다.

## 예문

나쁨:

```
결제 오류 수정 건 처리
```

좋음:

```
제목: 지수가 결제 모듈의 만료일 검사를 고친다
왜: 만료 당일 결제가 실패해서 하루 매출 집계가 어긋난다.
DoD: 수정 커밋과 통과한 테스트 링크가 있으면 닫는다. 기준은 8월 배포본이다.
다음 행동: 지수가 실패를 재현하는 테스트를 먼저 쓴다.
```

## 계보

태스크 서술의 원전 4개다. 이 문서는 원전의 구조에 STE-KO 문장 규칙을 포갠 것이다.

- GTD next action (David Allen). 태스크를 다음 물리적 행동으로 쓴다. <https://gettingthingsdone.com/2017/05/the-next-action/>
- INVEST와 SMART 태스크 (Bill Wake, 2003). 태스크 서술 기준의 원조 아티클이다. <https://xp123.com/articles/invest-in-good-stories-and-smart-tasks/>
- Shape Up의 pitch (Ryan Singer). 문제, 투입 상한, 해법 스케치, rabbit hole, no-go 다섯 요소다. <https://basecamp.com/shapeup/1.5-chapter-06>
- How to Report Bugs Effectively (Simon Tatham). 기대 상태와 현재 상태의 차이를 재현 가능하게 쓴다. <https://www.chiark.greenend.org.uk/~sgtatham/bugs.html>
